1. Estilos
Los estilos siguen con problemas en este caso creo que es porque las variables de css no se están cargando bien.

2. instalación de componente shadcn sigue fallando 
Intenta probar en local, npx shadcn@latest add https://cdn.jsdelivr.net/npm/@deandre-dock/buttons-shadcn@latest/dist/registry.json y mira que falta para que funcione, también se me ocurre que podriamos instalarlo normal como el otro en caso de que el registro sea muy completo, sino configuremos el proyecto para que funcione.